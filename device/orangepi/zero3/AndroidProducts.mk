# =============================================================================
# PulsarOS — AndroidProducts.mk
# Orange Pi Zero 3 (Allwinner H618)
# =============================================================================

PRODUCT_MAKEFILES := \
    $(LOCAL_DIR)/pulsaros.mk

COMMON_LUNCH_CHOICES := \
    pulsaros_zero3-userdebug \
    pulsaros_zero3-user
